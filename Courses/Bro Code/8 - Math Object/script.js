// Math = built-in object that provides a collection of properties and methods

let x = 3;
let y = 2;
let z = 1;

//console.log(Math.PI);
//console.log(Math.E);

// z = Math.round(x);
/* Rounds up */

// z = Math.floor(x);
/* Rounds down */

// z = Math.ceil(x);
/* The opposite of floor */

// z = Math.trunc(x);
/* Eliminates any decimal portion */

// z = Math.pow(x, y);
/* Raises base "y" to the power of "x" */

// z = Math.sqrt(x);
/* square root */

// z = Math.log(x);
/* Natural logarithm */

// z = Math.sin(x);
// z = Math.cos(x);
// z = Math.tan(x);
/* Trignometry functions */

// z = Math.abs(x);
/* Absolute Value */

// z = Math.sign(x);
/* Tells whether +ve or -ve of a number */

let max = Math.max(x, y, z);
let min = Math.min(x, y, z);
/* "z" is set to 1, "y" is set to 2 and "x" is 3 */

console.log(min);
console.log(max);