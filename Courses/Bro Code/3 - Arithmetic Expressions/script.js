/* Arithmetic operators = Operands (values, variables, etc.) 
                          Operators (+ - * /)
                          ex. 11 = x + 5*/

// let students = 20;

// students = students + 1;
    // students += 1;
    // studnets++;
// students = students - 1;
    // students -= 1;
    // students--;
// students = students * 1;
    // students *= 1;
// students = students / 1;
    // students /= 1;
// students = students ** 2;
    // students = ** 2;
    /* This is the exponent operator */

// let extraStudents = students % 3;
    // let extraStudents %= 3;
    /* Gives the remainder of any division */
    /* when doing the (modulus) its best to create a separate variable */

// console.log(students);
// console.log(extraStudents);

/*
    opertor precedence
    1. parenthesis ()
    2. exponents
    3. multiplication & division
    4. addition & subtraction
*/

let result = 1 + 2 * (3 + 4);

console.log(result);