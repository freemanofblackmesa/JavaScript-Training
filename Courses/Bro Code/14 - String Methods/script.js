// string methods = allow you to manipulate and work with text (strings)

let userName = "Bro Code";
let phoneNumber = "123-456-7890";

/* .charAt: is to get the first character of a string, */
//console.log(userName.charAt(0));

/* .indexOf: will return the first occurence of a character, */
//console.log(userName.indexOf("o"));

/* .lastIndexOf: will return the last occurence of a character, */
//console.log(userName.lastIndexOf("o"));

/* .length: is to show the length of a character */
//console.log(userName.length);


/* .trim: used to remove whitespace*/
//userName = userName.trim();

/* .toUpperCase: used to convert a string into uppercase letters*/
//userName = userName.toUpperCase();

/* .toLowerCase: used to convert a string into lowercase letters*/
//userName = userName.toLowerCase();

/* .repeat: creates new string by repeating the original string a specified number*/
//userName = userName.repeat(3);


/* .starstWith: to detemine if a string starts with a given character*/
//let result = userName.startsWith(" ");

/* endsWith: to detemine if a string ends with a given character-returns boolean*/
//let result = userName.endsWith(" ");

/* .includes: does the string contain any characters in the middle*/
//let result = userName.includes(" ");


/* .replaceAll: allows to replace any character with a desited one*/
//phoneNumber = phoneNumber.replaceAll("-", "");

/* padStart: make the string 15 characters by adding 0s from the start*/
//phoneNumber = phoneNumber.padStart(15, "0");

/* padEnd: make the string 15 characters by adding 0s from the end*/
//phoneNumber = phoneNumber.padEnd(15, "0");

console.log(phoneNumber);